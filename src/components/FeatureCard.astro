---
const { icon, title, description } = Astro.props;
---

<div class="flex">
    <div>
        <div class="flex justify-center items-center rounded-full bg-yellow shadow-lg w-10 h-10">
            <img class="w-5 h-5" src={icon} alt="Icon"/>
        </div>
    </div>
    <div class="ml-4 mt-1 mr-2">
        <h3 class="text-lg font-bold">{title}</h3>
        <p class="mt-2 text-md">{description}</p>
    </div>
</div>